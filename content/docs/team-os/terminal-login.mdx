---
title: "Terminal login"
description: "Sign in to Team OS from the terminal."
---

Run terminal commands from `command-centre` in the Agentic OS repository.

Keep the `--` after `npm run team`. It tells npm to pass the rest of the
command to Team OS.

```bash
cd command-centre
npm run team -- login --api-url https://team.example.com --email user@example.com --password "<password>"
```

If your email belongs to more than one team, pass the team slug or team ID:

```bash
npm run team -- login --api-url https://team.example.com --email user@example.com --password "<password>" --team acme
```

Expected output:

```text
Signed in as user@example.com
Team: Acme
```

The CLI stores a user-scoped Team OS session token outside the repository. It
does not save your password.

## Check your session

Show the server-resolved user, team, and role:

```bash
npm run team -- whoami
```

Expected output:

```text
Signed in as: user@example.com
Team: Acme
Role: member
```

Show the clients granted to your account:

```bash
npm run team -- clients
```

Example output:

```text
acme	read	Acme
globex	write	Globex
```

If you have no client grants, the command prints:

```text
No clients are available for this account.
```

That does not always mean your login failed. It can mean an owner or admin has
not granted you access to a client yet.

## Sign out

Sign out from the terminal:

```bash
npm run team -- logout
```

Expected output:

```text
Signed out.
```

If a saved session expires, run login again with the same server URL, email, and
password.

Next: [Saved config directory](/docs/team-os/saved-config)
